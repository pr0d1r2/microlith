# nanokit — the cavekit SPEC format, enforced

Self-contained spec. `nanokit` owns the cavekit SPEC format and the CPU operations over it. It exists because that format had TWO hand-maintained implementations and no home: a checker in a host workspace that does not travel, and a ported copy in a consumer's test suite. They disagreed, both gates stayed green, and 88 tasks belonged to no milestone before anything noticed. One implementation, or the rule is decoration.

Lineage: the format is cavekit's, and every measurement below was taken in `itok`, the first consumer, which is public alongside this repo. A SEE-ALSO, ⊥ load-bearing (V19).

## §G GOAL

Keep a `SPEC.md` CORRECT, SMALL & ADDRESSABLE: enforce the format, minify it losslessly, derive its anchors -- CPU only, no model.

## §C CONSTRAINTS

- Rust. One bin: `nanokit`, plus the lib every consumer calls. MIT licensed.
- CPU ONLY. ⊥ inference, ⊥ network, ⊥ a model. Deterministic, or it does ⊥ ship.
- `FORMAT.md` is NORMATIVE & lives HERE. Every consumer reads THIS copy -- it was homeless (a plugin dir), which is half of why the two implementations drifted.
- Zero host-internal deps ∴ any repo adopts it as an ORDINARY dependency.
- The spec CAPS ITSELF from commit one, ⊥ after the growth has happened.
- NO FLEET REPO IS NAMED HERE, in the source, or in a COMMIT MESSAGE. This crate is measured against ~51 private specs & will be published ∴ every count, dialect & bug record cites them ANONYMOUSLY -- `a fleet spec`, `three fleet specs`, `0 of 6 · 0 of 12 · 0 of 6`. The EVIDENCE survives the anonymisation; the names add nothing a reader of this repo can use. Git history is ⊥ exempt ∵ it is the part that cannot be edited after publishing.

## §I INTERFACES

- `nanokit fmt [--check] <path>` — lossless minify: unwrap hard wraps, enforce the line cap. Default REWRITES; `--check` reports & exits 1 on drift (`rustfmt`'s grammar).
- `nanokit check [--records <file>] [--format human|json] <path>` — the structural rules, all from the TEXT ALONE: sections present & ordered (V11) · ids unique (V12) · citations resolve (V13) · rows sorted (V14) · every task in exactly one milestone (V15) · a task status ∈ `.` `~` `x` (V25). Each violation carries `rule` · `line` · `why` · RANKED `directions`, each `Mechanical` | `Judgment`; `--format json` renders the SAME anatomy ∴ an agent never parses prose, & an unknown format is a USAGE error, ⊥ a silent fall back to prose. `--records` adds V16, whose baseline the CALLER owns ∵ survival across edits is ⊥ derivable from one file; the lib also exposes `records_survive(text, expected)` for a consumer keeping that list as a const.
- `nanokit anchors <path>` — derive the `§S.n` addressing FORMAT.md defines.
- `nanokit derive <path>` — sizes · citation graph · orphan invariants. Report-only.
- `--verbose` on EVERY verb: opts OUT of silence-is-success (V10), ⊥ adds noise to it. What it says differs ∵ the silence differs: `fmt`/`check` are MUTE when clean ∴ verbose CONFIRMS what was examined; `derive`/`anchors` already speak ∴ verbose DEEPENS -- per-statement sizes biggest-first, and full text ⊥ a 60-char gist. A flag that printed the same thing louder would be decoration.
- Exit: 0 ok · 1 drift or violation · 2 usage.

## §V INVARIANTS

V1: **`fmt` is LOSSLESS, and PROVABLY so.** Normalizing all whitespace in the input and in the output ! yield IDENTICAL strings, asserted BEFORE the write is allowed. A formatter that CAN drop a fact is one nobody may run unattended, & "I read the diff" is ⊥ a proof: an assertion from inspection has a measured record of being wrong, while a claim backed by a RUN needs no such gate. MEASURED: this exact assertion is what made a 1167→276-line transform safe to run on a 92kB law file.
V2: **`fmt` is IDEMPOTENT.** `fmt(fmt(x)) == fmt(x)`, tested, ∵ a formatter that keeps changing its mind cannot be a `--check` gate: CI would fail on the file the last run produced.
V3: **one line per STATEMENT; hard wrap is ⊥.** grep & every string-anchored edit are LINE-oriented ∴ a wrap defeats both SILENTLY. MEASURED: 101 of 101 invariants in a real spec spanned >1 line ∴ `grep V47` returned a FRAGMENT, a phrase crossing a wrap returned NOTHING, and an edit whose anchor had been rewrapped did nothing while the suite stayed green. Cost agrees: unwrapping saved 842 tokens against +38% on a one-word diff hunk (the line is the DIFF unit ∴ a paragraph re-sends TWICE), break-even ~7 edits per session.
V4: **the CONTINUATION rule is PAIRWISE, ⊥ per-line.** A wrap is a non-blank line FOLLOWING a non-blank line that opens nothing of its own. A blank-separated paragraph & the text under a header carry NO marker & are whole statements ∴ a per-line predicate rejects them -- which is exactly how the first version of this rule failed, on a spec's own intro paragraph.
V5: **the line CAP is the OPPOSITE failure & is set from MEASURED data.** Unbounded lines are base64 · minified blobs · a pasted transcript. Set the cap ABOVE the measured post-`fmt` maximum with real slack (~12%), per V21's CEILING rule -- which owns that reasoning & the evidence for it.
V6: **CPU ONLY -- inference stays a SKILL.** Every operation here is derivable: sizes, the citation graph, orphans, anchors, the unwrap. That makes them deterministic, free, offline & testable. A rewrite that needs judgement is a `/spec` call by a human or an agent, ⊥ a hidden model call inside a formatter.
V7: **ONE implementation; a consumer CALLS it, ⊥ re-ports it.** Two hand-maintained copies of one rule set is the defect this crate exists to end. A copy is permitted ONLY as a VERBATIM vendored file, re-synced on upgrade & marked as vendored.
V8: **`FORMAT.md` is the vendored REFERENCE & ships HERE; §V is NORMATIVE for what this tool ENFORCES.** cavekit 4.1.0, byte-identical (Julius Brussee, MIT), mode 444 ∴ a tool citing the format can READ it. The two are ⊥ equals & the split is the point: FORMAT.md's prose restates V11, V12 & V25 while §V is a SUPERSET -- V13, V14, V16 & the losslessness proof have NO upstream counterpart ∴ treating both as law is two statements of one format, & the one with the RUNNER wins (V17). A format cited by every command & living in nobody's repo is unreadable exactly when needed -- the drift behind 88 orphan tasks was compounded by a dead reference nobody could follow, so a guess got made instead of a read.
V9: **the spec CAPS ITSELF, from commit ONE.** `.context-limits` carries a ceiling before the file grows into it. EVIDENCE: the first consumer set its first ceiling at 19,542 -- the size the file ALREADY was -- then raised it four times, to 27,500. A ceiling that arrives after the growth RATIFIES it rather than catching it.
V10: **report-only by DEFAULT; the gates are named & closed.** `fmt --check` and `check` gate; `derive`, `anchors` and bare `fmt` do not. A tool that rewrites law is one a user ! invoke deliberately.
V11: **sections are ORDERED & no ITEM outlives its header**, per FORMAT.md: §G · §C · §I · §V · §T · §B. ABSENCE is LEGAL -- 4.1.0 says so & only 41 of 51 fleet specs carry §G ∴ demanding all six rejected LEGAL specs (B4's shape). What presence was FOR survives narrower: `V1:` rows with no §V is the defect; a spec with no bugs needs no §B. LABEL-AGNOSTIC: only 16 of 51 spell `## §V INVARIANTS` exactly, 9 write `Invariants` & 7 `— Invariants` ∴ matching the whole string called a section MISSING while it sat right there. V11 asks only DOES IT EXIST & WHERE; the label is V27's. UNKNOWN letters pass untouched -- §D · §E · §F · §O · §P · §X are all in fleet use.
V12: **ids are UNIQUE & NEVER REUSED; a GAP is fine.** A skipped number costs nothing; a REUSED one silently redirects every citation that pointed at the old meaning.
V13: **every citation RESOLVES; a citation is a `V<n>` OUTSIDE backticks.** A dangling `V99` is a pointer into nothing & it reads as authoritative -- the most expensive kind of wrong, ∵ nobody checks a reference that looks deliberate. INSIDE backticks it is a LITERAL, ⊥ a citation: FORMAT.md already reserves them for verbatim text ∴ the boundary is READ OFF the format, ⊥ invented for this rule -- one invented in code is one the next consumer re-derives differently. MEASURED: B3 names the three mentions this caught.
V14: **rows appear in SORTED id order, and a suffixed id RIDES its base** (`T30a` sorts after `T30`, ⊥ lexically). MEASURED: four rows sat out of order for weeks ∵ rows get appended wherever is convenient, and an out-of-order block RENDERS identically to a sorted one ∴ nothing but a check sees it.
V15: **every task belongs to EXACTLY ONE milestone**, read from the THIRD pipe field of an `| M<n> |` row, with RANGES expanded (`T1-T4, T12`). Named by the original checker as the rule most often broken and invisible without a check; it was right -- 88 tasks belonged to none while two gates stayed green. Ranges are the format's own affordance & the reason the mapping is cheap to maintain: not knowing about them is why the column once got judged a burden and deleted. SCOPED to specs that OPTED IN: milestones are an EXTENSION -- canonical §T has none & 48 of 51 fleet specs carry no `| M<n> |` row ∴ this fired `in no milestone` on EVERY task in them, one violation per row, for declining an optional feature. A check that loud is one nobody reads. ONE milestone row is the opt-in signal, & it is the right one: adding the first is the moment the claim starts being MADE.
V16: **considered-and-CLOSED records survive: an option REJECTED, or DEFERRED under a stated TRIGGER.** Recorded with its reason & the trigger that would reopen it is what makes a decision AUDITABLE rather than merely obeyable. Compaction ! never trade these for bytes. THE TRIGGER TELLS THEM APART & this spec already writes both: T14's `PreToolUse` hook is REJECTED -- trigger `⊥ ever, while V6 stands` -- while T10's autodetection & T14's `nk:` skills are DEFERRED, each naming a CONDITION. A deferral is MORE deletion-prone than a rejection ∵ it reads as an UNBUILT FEATURE, & a compaction pass drops it thinking `⊥ built = ⊥ needed`. Checked by NAMED records, ⊥ a word count: an arbitrary threshold either fires on nothing or fires on prose edits that changed no decision. ⊥ §T: a row is `T<n>|status|text|cites` with done-when on the MILESTONE ∴ a deferral moved there LOSES its trigger & keeps only `maybe someday`, & no status in V25's set expresses `unplanned until X` -- correctly, ∵ it is ⊥ a task state.
V17: **a rule and its CHECKER land in ONE commit.** A rule with no runner is a LIE that reads as law -- worse than no rule, ∵ it provides false assurance. And a checker landing on a file that violates it is RED AT BIRTH ∴ the rewrite ships with it.
V18: **a guard is proven by PLANTING a violation**, ⊥ by reading it. A guard that has never rejected anything is indistinguishable from one that CANNOT. Every check here ships with its planted counter-example AND a companion that accepts every real shape, so it cannot pass by rejecting everything.
V19: **an id is QUALIFIED whenever it crosses a namespace -- INBOUND as see-also, OUTBOUND in what we EMIT.** Inbound: name the repo (`itok's V82`), never a bare `V82` -- this spec owns that namespace, so an unqualified number reads as OURS & breaks. Outbound is the SAME failure mirrored & it is the one that bites a CONSUMER: `check` prints its verdict against THEIR file ∴ a bare `V13` there names one of THEIR rules. MEASURED on the first consumer: every id this crate can emit (V1-V5, V11-V16) collides with a live unrelated invariant -- 100%, ∵ every consumer is a caveman spec numbering from V1. Human output carries `nanokit/V13`; json keeps the bare id ∵ the FIELD (`rule`) names the namespace structurally -- the porcelain/plumbing split, ⊥ an exemption. And every rule stands on evidence RESTATED here: a reader ! never fetch another repo to understand a rule, even when that repo is public. Lineage lives in git history & the commit trail.
V20: **PUBLISH before DEPEND.** A consumer takes this crate from a REGISTRY, ⊥ a relative path: a path dep dangles the moment either repo is extracted or moved, which is the exact wart it would be adopted to remove. Until published, a consumer keeps its own copy & uses `nanokit` as a SECOND OPINION -- two implementations by design, with drift visible, beats one implementation that cannot be built.

V21: **a threshold is MEASURED HERE, and a FLOOR ratchets while a CEILING breathes.** Copying a number from another project is the classic failure: the first consumer's gate floors coverage at 98, and this crate's own measures 94.68% (format.rs 100 · lib.rs 94.44 · main.rs 89.91) ∴ a copied floor would have failed this repo on commit ONE. The two directions take OPPOSITE slack & are ⊥ one discipline: a size CEILING wants ~12% headroom ∵ a ceiling a hair above current turns every legitimate addition into a raise; a coverage FLOOR wants ~1 point ∵ it is a RATCHET, and a floor far below current gates nothing. NAME the gap rather than hiding it: `main()` and the I/O arms are structurally unreachable from lib tests, & only a process-boundary test moves them.
V22: **ONE gate definition, many callers; the workflow holds ORCHESTRATION only.** `hk.pkl` owns WHICH command, WHICH flags, WHICH order & the coverage floor; `ci.yml` owns the runner, the toolchain & the checkout ∵ that half has no local equivalent to disagree with. ∴ pre-commit, pre-push & CI reach ONE copy and the ops CANNOT drift. ELIMINATION beats policing -- a drift guard only fires AFTER someone has written the second copy. The gate of record stays REPRODUCIBLE with plain cargo: every step is a command a contributor can paste with nothing but rustup ∴ the hook runner is a CONVENIENCE, ⊥ a dependency of the crate, and no verdict may rest on its own logic.
V23: **a hook DEGRADES when its runner is absent; it ⊥ BLOCKS.** A gate exists to stop bad commits, ⊥ to stop git. Runner missing -- a fresh clone, outside the dev shell, a container -- ⇒ ONE line to stderr and exit 0: LOUD about being skipped, ∵ a SILENTLY skipped gate is indistinguishable from a passing one. Hard-failing instead makes the repo unusable for anyone who has ⊥ installed the toolchain, the wrong failure mode for a convenience when the gate of record is CI (V22). ∴ the hook command is written BY HAND: an installer writes a command assuming its own binary is on PATH, and outside the shell it is not, so every git command in the repo fails rather than merely running ungated.
V24: **nanokit DOGFOODS itself, using the binary being BUILT.** Its own `SPEC.md` is the first file it formats & caps ∴ a rule this crate cannot pass is a rule it cannot ship, and the spec & the code cannot drift apart without the gate noticing. The dev shell supplies `nanokit` as a `cargo run` SHIM, ⊥ a built package: a package must COMPILE before the shell opens ∴ one error locks you out of the shell you need to fix it, and a built binary is stale against the working tree, which is the opposite of dogfooding. HISTORY: was v0.1.0's V10, dropped in a renumber, then spent a day as runners with no text -- B2 carries the record & the rule it produced.

V25: **a task STATUS is `.` todo | `~` wip | `x` done, & nothing else**, read from the SECOND pipe field of a `T<n>|` row, per FORMAT.md. A status outside the set RENDERS as ordinary text ∴ a table full of them looks fine, while every runner that reads the column -- a dashboard, a `/build --next`, a human skimming for what is open -- silently disagrees about the state of the work. FOUND MISSING: the host checker had this rule & nanokit did ⊥ (B4), which is why T7 could ⊥ delete it yet.
V26: **a DECLARATION is recognised past a markdown LIST MARKER** (`- ` · `* ` · `+ `), with or without the colon. FORMAT.md's example is COLUMN ZERO & that stays canonical -- but the id grammar reading only column zero made `check` report 1,134 DANGLING CITATIONS against invariants declared three lines above (B8). MEASURED across the fleet: 1,097 of 1,750 declarations carry a bullet ∴ the majority form. Recognising it is ⊥ blessing it: canonical form is T17's rule & T16's migration, & the split is deliberate -- a checker that MISREPORTS a defect is worse than one that misses it, ∵ it sends every reader to the wrong line.
V27: **a canonical LETTER carries its canonical MEANING.** The header ! CONTAIN its canonical word -- `goal` · `constraint` · `interface` · `research` · `invariant` · `task` · `bug`, matched as a STEM ∴ `Bugs` · `bug log` · `— Bugs / Known Issues` all pass & qualifiers follow freely. WHY: FORMAT.md's ADDRESSING promises ZERO AMBIGUITY -- `§V.2` ! mean one KIND of thing in every repo, or a citation that TRAVELS resolves to a different concept, silently. V11 asks whether the section is THERE; this asks what it is CALLED, & splitting them is what lets each say something TRUE (before T15 they were one rule & a title-cased header read as MISSING). An UNKNOWN letter has no canonical word ∴ ⊥ this rule's business. The FIX classification carries the judgement, ⊥ the rule: `SYNONYMS` is an AUDITED table, one row, & absent-from-it FAILS CLOSED as a concept collision.
V28: **`fmt` never LOSES a statement**, by TWO mechanisms that catch DIFFERENT failures. (a) a marker is read on the TRIMMED line ∴ an INDENTED list item opens its own statement & is ⊥ merged into the line above -- this is what fixes B12. (b) the id SET after a format ⊇ before, ASSERTED before the write -- this catches the GENERAL case, a VISIBLE declaration merged away. (b) could ⊥ have caught B12: an indented id was never DECLARED to begin with ∴ no id-set proof can miss what it never saw. WHY either is needed: V1 compares NORMALISED WHITESPACE ∴ it is structurally BLIND to a MERGE -- the proof PASSES while a statement stops existing. ⊇ ⊥ ==: `fmt` ? GAIN an id by dedenting a hidden one (T19), & that is a REPAIR. V1 says no CONTENT vanished, V28 says no STATEMENT did, & neither implies the other.
V29: **a rule that reads MARKDOWN STRUCTURE is verified against a FIXTURE per construct FORMAT.md permits**, ⊥ against our own spec alone. V24's dogfood proves rules against ONE FILE ∴ it is BLIND to every construct that file happens ⊥ to use -- & that is ⊥ a theory: B12 (indented items) · B13 (ordered lists) · B14 (fenced blocks) are ONE root cause, our SPEC.md contains NONE of the three, & the dogfood caught ZERO of them while the corpus caught ALL. The fixtures SHIP (§C forbids naming the private corpus) ∴ a consumer re-runs the coverage instead of trusting a run they cannot see. BIDIRECTIONAL: a name with no file & a file with no name BOTH fail ∴ adding a construct FORCES a fixture, & the guard cannot rot into a directory of old attempts.

## §T TASKS

| id | scope | tasks | done-when |
|----|-------|-------|-----------|
| M1 | the formatter -- the capability that exists nowhere else | T1-T3 | `nanokit fmt --check SPEC.md` green on this repo, losslessness asserted before every write |
| M2 | the gate -- this repo defends itself before it grows | T6 | `hk check --all` exit 0, hooks installed by shell entry, coverage floor measured HERE |
| M3 | the checker -- one implementation of the format rules | T4-T5, T9-T11 | `nanokit check` enforces V11-V16, each proven by a planted violation |
| M5 | mechanical editing -- the agent stops doing raw operations on the spec | T12-T14 | every mutation goes through one verified write path, and the skills that already edit SPEC.md call it |
| M6 | the fleet -- one checker, 51 specs | T15-T19 | `check` passes on every fleet spec, or names it a deliberate exception |
| M7 | real-world markdown -- the corpus as the test, not our own file | T20-T22 | every construct FORMAT.md permits has a fixture, and the two declaration dialects convert |
| M8 | planning the work -- what the tool may COMPUTE, and what it must not | T23 | the unclaimed range is reported, and no milestone is invented |
| M4 | adoption -- consumers stop carrying copies | T7-T8 | published, the first consumer depends on the registry crate and deletes its ported copy |

T1|x|`fmt` core: unwrap hard wraps, pairwise continuation rule, line cap; lossless proof asserted BEFORE the write; idempotence tested|V1,V2,V3,V4,V5
T2|x|`fmt --check`: report drift & exit 1, ⊥ rewrite; the `rustfmt`/`nixfmt` grammar so it needs no teaching|V10,V2
T3|x|`.context-limits` at 4k from commit one + `tests/dogfood.rs`: this SPEC.md is formatted, under the cap, and carries real slack -- asserted against the library being built|V9
T4|x|`check` verb, RUN & green: V11-V16 as pure fns over `&str` in `src/check.rs`, each with a PLANTED violation & a companion accepting every real shape; `nanokit check --records .spec-records SPEC.md` exit 0, gated by hk & by `tests/dogfood.rs` (V24). Rules restated HERE, ⊥ referenced, ∴ deleting the consumer's copy in T7 costs no evidence (V19). `src/id.rs` holds the id grammar ONCE ∵ `fmt` & `check` both need it & two parsers would BE the defect this crate ends (V7). V16 takes the baseline from the CALLER (`--records`) ∵ survival across edits is ⊥ derivable from one file. MEASURED: coverage ROSE 94.68→97.12% ∵ the arg handling arrived WITH its tests. First real run found 3 false positives ⇒ B3 & V13's backtick boundary|V7,V11,V12,V13,V14,V15,V16,V18
T5|x|`derive` + `anchors`, RUN & green: sizes · citation counts · orphans · `§S.n` addressing, all report-only ∴ exit 0 EVEN WITH FINDINGS -- an orphan is a question for a reader, ⊥ a build failure, and a gate would answer it by fiat (V10). Sizes are CHARS: ⊥ bytes (this format is dense with 3-byte symbols) & ⊥ tokens (a tokenizer is a dependency; `itok` owns that, ⊥ us). `anchors` emits FORMAT.md's ORDINAL with the id it resolves to BESIDE it, ∵ an ordinal SHIFTS when an earlier id retires -- B2 one level out -- so drift is SHOWN, ⊥ designed around; a `| M<n> |` row is furniture ∵ counting it would push every `§T.n` off its task id on a WELL-FORMED spec. FOUND on first run: V8 orphan (uncited) · §B rows cost 809 chars mean vs §V's 389 ∴ a bug record outweighs the rule it protects|V6,V10,V18
T6|x|the gate, RUN & green: `hk check --all` exit 0 · `nix build` · a fresh shell installs `pre-commit`+`pre-push` · hk absent ⇒ ONE stderr line, exit 0 (proven by PLANTING an empty PATH, V18). Coverage re-measured AFTER the gate existed -- 94.68% lines, unmoved by doctest/rustdoc ∴ floor 94 stands as a measurement, ⊥ a prediction (V21). `pkl/` is vendored VERBATIM ∴ exempt from every formatter AND from `typos` (B1). `actionlint` landed WITH the live workflow, ⊥ before it. DROPPED as consumer-shaped, ⊥ forgotten: the cassette/network axis · `--no-default-features` (no features here) · `package` (nothing published until T8) · `shellcheck` (no shell scripts -- a linter with nothing to lint is a rule with no runner) · `fetch-depth: 0` (no test reads `HEAD~n`)|V21,V22,V23,V10,V18
T7|~|absorb the host checker & the consumer's `tools/specfmt`. DONE: `specfmt` DELETED & itok's second opinion now shells out to `nanokit check`, out of process ∴ ⊥ a dependency (its ported guards stay until T8, per V20). It EARNED the swap on run one -- nanokit flagged itok's §B rows out of id order, which NEITHER its ported guards NOR the host checker look at (both order V & T only) ∴ nanokit is strictly stricter, MEASURED ⊥ claimed. BLOCKED, ⊥ forgotten: `a fleet repo's host-checker` has a SECOND consumer I had ⊥ counted -- its own test gates that repo's SPEC.md. `nanokit check` passes that spec (exit 0) ∴ it CAN replace it, but ⊥ until a fleet repo may depend on a REGISTRY crate (V20) ⇒ T8 first. B4's rule applied to itself: `delete X` needs the evidence X is replaced for EVERY consumer, ⊥ just the one in view|V7,V17,V20
T8|.|publish, then the consumer depends on the registry crate and drops its ported guards -- in that order (V20). Smaller and zero-dep ∴ this is also the publish REHEARSAL for the larger consumer's own release|V20,V7
T9|x|PARITY with the host checker, MEASURED ⊥ believed (B4's own rule): all 5 gaps closed & re-compared -- task status (V25) · `line` · stable `rule` id · `Mechanical`\|`Judgment` · JSON. A `Violation` carries the failure ANATOMY: `rule` (match on THIS; prose is ⊥ stable) · 1-based `line`, 0 = document-scoped · `why` in one clause · RANKED `directions`, correct fix FIRST. V16 offers NO mechanical direction BY DESIGN ∴ an agent hitting it must STOP -- which is what trading a record for bytes ought to feel like. `--format json` renders the SAME anatomy, hand-encoded ∵ zero deps ∴ escaping is TESTED (quote · backslash · control · newline), validated through a real parser. Unknown format = USAGE error, ⊥ silent prose. CAUGHT while wiring: `positional` skipped one flag's value ∴ `--records r --format json SPEC.md` would have taken `json` as the path|V25,V10,V18,V7,V17

T10|x|SYNC the format to cavekit 4.1.0, VENDORED VERBATIM (V7) -- ⊥ forked, ⊥ generated. `FORMAT.md` is cavekit's byte-for-byte (Julius Brussee, MIT) & V8 is amended: vendored REFERENCE vs §V NORMATIVE. 4.1.0 adds `§R RESEARCH`, OPTIONAL, between §I & §V ⇒ `SECTIONS` gains a 7th entry & the id grammar gains one LETTER ∴ V12 & V14 covered R rows FREE, as argued -- `R1|topic|finding|src` is an ordinary pipe row & needed no rule of its own. V11's reformulation landed EARLIER, in T15, ∵ the corpus proved it was ⊥ merely tidier. NO divergence: `§I INTERFACES`, canonical -- the singular was ratified on n=2 & the fleet OVERTURNED it 18 to 2 (B4). DROPPED: multi-version support & AUTODETECTION. 4.1.0 ⊇ 4.0.0 for every rule checked here ∴ one rule set accepts both & two would BE §G; detection is unsound regardless ∵ §R is OPTIONAL ∴ its absence proves NOTHING. TRIGGER to reopen: upstream ships a BREAKING change -- then DECLARATION, ⊥ a guess. FOUND while wiring `SECTIONS`: B9 & B10|V7,V8,V11,V12,V14,V16,V17,V18

T11|x|`dup`: cross-statement DUPLICATION, as a section of `derive`. WHY: `.context-limits` names "the duplication pass" 4× as the gate on every ceiling raise & it has NO RUNNER -- it is run BY HAND ∴ a procedure this repo already relies on is V17's lie that reads as law. MEASURED twice: V5/V21 shared 14 words (missed by 3 read-throughs ∵ a repeated clause reads fine in both places) · V13/B3 share 11 in a spec deduped HOURS earlier. Threshold 8 words, MEASURED ⊥ chosen (V5): 5-6 admit coincidences · 7-10 yield ONLY the genuine hit · 12 misses it. NO knob -- a threshold with a flag is one someone lowers to make a report quiet. ⊥ a new verb: `derive` already answers how-big · what's-biggest · what's-uncited, & this is what's-said-TWICE. ⊥ a `check` rule ∵ duplication is a SMELL, ⊥ a violation -- a spec ? legitimately repeat itself. Report-only, exit 0 (V10): names BOTH ids, recommends NEITHER ∵ which copy dies is JUDGEMENT. Strips backticked spans, REUSING V13's boundary ∴ a shared command name is ⊥ shared reasoning; case-INSENSITIVE & MAXIMAL phrases only -- both were bugs in the hand-run script. DOGFOOD (V24): our own spec carries NONE ≥8 ∴ V13/B3 is cut in the SAME commit or the guard is RED AT BIRTH (V17). RED AT BIRTH as predicted: 1st run on our own spec found 4 pairs, all written TODAY -- T18/B10 (10 & 9 words) · T15/V11 · V27/V11 (9). Cut HERE ∴ green, & the 1st found by a RUNNER ⊥ by hand|V3,V5,V6,V10,V13,V17,V18,V24

T12|.|MUTATION verbs, every one through ONE verified write path ∴ no verb can FORGET a check -- B7's shape (a step that forgot to trigger), applied before the bug instead of after. `read <id>` (also `§S.n`) · `next <kind>` → the next free id · `add <kind> ".."` APPENDS at that id ∴ ⊥ insert, making B2's rule STRUCTURAL rather than remembered · `after <id> ".."` takes a SUFFIXED id (`B1a`) when something sorts after it, else the plain next ∴ nothing renumbers · `do <id>` → `~` · `done <id>` → `x`. THE WRITE PATH, in order: PROVE the edit ADDITIVE (original text is a PREFIX & exactly one line added -- `add`'s answer to V1) → `check_spec` → REFUSE on any violation or an over-cap line (V5) → write → REPORT duplication of the NEW statement ONLY (T11's fn, scoped ∵ a pre-existing pair is `derive`'s job), ⊥ refuse ∵ duplication is a SMELL (V10, exit 0). Two result classes: violations REFUSE, smells REPORT. The ONE-WIP rule lands with its checker (V17) ∴ unnumbered here: at most one `~` at a time. `done` ALSO wants the task `~` first -- but that is a WRITE-PATH guard, ⊥ an invariant, ∵ the file holds CURRENT state & nothing can see a `.`→`x` jump after the fact (V16's asymmetry). Verify: a planted violation per verb (V18) · `add` on our own spec is provably additive & leaves `check` green|V1,V5,V10,V12,V14,V17,V18,V25

T13|.|`check --fix`: apply ONLY the directions the tool can COMPUTE (B5's corrected test), report the rest untouched. The FOUR survivors are the ones B5 names. NOTHING else is applied ∴ V13 & V16, which carry none, STOP an agent outright -- T9 states why that matters. `--fix <id>` scopes to one item. Goes through T12's verified write path ∴ a fix that would break a DIFFERENT rule is REFUSED, ⊥ written -- a repair that trades one violation for another is a worse state than the one it found. ⊥ a new verb: `fmt` writes by default & `--check` is its read-only mode; `check` reads by default & `--fix` is its writing mode. SYMMETRIC, & the flag stays attached to the thing that PRODUCED the directions. `fix` alone was REJECTED as a verb ∵ §B's own columns are `id\|date\|cause\|fix` ∴ `nanokit fix B2` reads as "set B2's fix field". The rule `Mechanical` ! mean COMPUTABLE gets its RUNNER here & is numbered then (V17). Verify: each of the 4 applied & the result passes `check` · a Judgment direction is NEVER applied, planted per rule (V18)|V10,V17,V18

T14|.|SUPPORT the skills that already edit SPEC.md; ⊥ police them. `ck:build`'s status flips call `do`\|`done`; `ck:spec`'s §B append calls `next`+`add`. The SKILL keeps the judgement, the TOOL does the id arithmetic & placement -- which is the whole division this crate rests on (V6: inference stays a SKILL). MEASURED need, ONE session: an agent inserted 3 rows in the WRONG place -- B2 before B1 · V24 before V23 · T9 before T8 -- each needing a SECOND script to move it, WHILE writing the checker for that rule. `add`\|`after` computing placement makes all 3 impossible. DROPPED, & this record is worth more than the task: a `PreToolUse` hook DENYING Edit/Write on SPEC.md. It INVERTS V23 -- a hook DEGRADES, ⊥ blocks -- & would block `/spec`, the very tool nanokit DEFERS to for judgement edits ∴ the spec becomes mechanically-appendable & never revisable. MEASURED against itself: 7 judgement edits in one session (dedup V5/V21 & V13/B3 · reformulate V13 · compact T6 & V24 · amend T10 ×2) that NO verb can perform -- the hook would have blocked the compaction discipline that produced THIS task's evidence. TRIGGER to reopen: ⊥ ever, while V6 stands. DROPPED: a parallel `nk:` skill set mirroring the CLI 1:1 ∵ a skill that only shells out earns nothing. TRIGGER: a composite needing >1 verb with judgement BETWEEN them. ⊥ startable until T12 ∵ a skill naming unbuilt verbs is V17's lie. Touches cavekit's skills ∴ its own confirmation, ⊥ this repo's gate|V6,V16,V17,V23

T15|x|CALIBRATE the rules against the CORPUS. Rules derived from n=2 (this repo & itok) & generalised without measuring ∴ 2 of 51 fleet specs passed. RECORDED, ∵ the number is the calibration's only honest measure: **2 -> 35 pass** (28 non-vacuous; 7 have no items ∴ pass by having nothing to check) & **1,147 -> 51 violations**. THREE miscalibrations, each a TRUE rule with a FALSE message: V11 demanded all SIX sections PRESENT (only 41 of 51 have §G) & matched the header STRING (only 16 spell `## §V INVARIANTS` exactly, 9 `Invariants`, 7 `— Invariants`); V15 fired on EVERY task in the 48 specs with no `| M<n> |` row; & the id grammar read COLUMN ZERO only, ∴ 1,134 bogus dangling citations (B8). The 51 REMAINING: 22 REAL defects (9 V11 · 5 V14 · 8 V13) & 29 a NAMED EXCEPTION -- two DECLARATION DIALECTS, a markdown TABLE `| V1 | ... |` (20) & a plain NUMBERED LIST (9), both mechanically migratable ∴ T16's, ⊥ a rule to loosen. Also found & left to T16: `## S.G` ASCII for `## §G`. EXTENSION sections tolerated: §D · §E · §F · §O · §P · §X|V11,V15,V18,V24,V26

T16|x|`migrate`: normalise section HEADERS to canonical 4.1.0, & ⊥ one byte dropped. THREE cases, & V27's audited `SYNONYMS` table decides WHICH ∴ the judgement is DATA, ⊥ inference: (1) case & punctuation only (`— Interfaces` · `Interfaces`) → rewrite SILENT, nothing to preserve; (2) BARE `## §V` or a LISTED synonym → canonical header & the original kept beneath as `Migrated by nanokit from: <original>`; (3) a COLLISION (`§V VERSIONING`) → ⊥ TOUCHED, reported. SUPERSEDES this row's 1st draft (report all 13, touch none): V27 made the split MECHANICAL. WHY (3) stays untouched: annotating a collision keeps every char & INVERTS the meaning -- a build section under a BUGS header -- lossless in BYTES, wrong in SUBSTANCE, & it PASSES the byte proof. PROOF, asserted BEFORE the write: every ALPHANUMERIC run of the original survives, CASE-INSENSITIVELY, in the canonical label or the note. ⊥ V1's whitespace identity -- migrate CHANGES text ∴ needs its own, & case-folding is what makes (1) provable. `## §T55-PLAN` is ⊥ a header ∴ letter-then-DIGIT rejected. Idempotent (V2): a 2nd run sees canonical, does nothing, adds no 2nd note. DECLARATION bullets ⊥ migrated -- B8's linter question unsettled ∴ a rewrite markdownlint REVERTS. MEASURED over 51: 26 REWRITTEN · 14 NOTES · 6 DECLINED · 0 refusals · 0 changed on a 2nd run (V2) · V27 31 -> 18, & the 18 are EXACTLY the collisions ∴ it fixed all it claimed, none it disclaimed|V1,V2,V6,V18,V27

T17|x|A CANONICAL LETTER CARRIES ITS CANONICAL MEANING -- landed as V27 with its checker (V17). MEASURED over the fleet: 31 violations, ⊥ the 25 predicted -- the extra 6 are BARE headers (`## §V`, no label at all) that the earlier regex skipped ∴ the prediction was low by exactly the shape it could ⊥ see. The SPLIT came out as designed & is where the judgement lives: 13 MECHANICAL (7 synonyms + 6 bare) · 18 JUDGMENT = 6 repos × 3 collided sections. Low-noise as argued: 236 of 261 headers already carry the word, & `— Bugs / Known Issues` · `— Invariants (rules research must hold)` · `Research — gem/tooling evaluation` all PASS. The synonym table got a RUNNER TODAY rather than waiting for T16: it picks the fix KIND ∴ ⊥ a rule with no runner (V17). A synonym is repairable -- canonical word + the old label kept as a note, lossless; a collision is ⊥, ∵ the CONTENT ! move & no header rewrite moves it|V11,V17,V18,V24,V26
T18|.|`migrate` MOVES a misplaced section into canonical order -- mechanical, & the second mutation class after T16's line rewrite. Cut header-to-next-header, reinsert at its canonical rank ∴ a pure PERMUTATION of blocks & the proof holds BY CONSTRUCTION: same blocks, same bytes, new order ∴ V1's whitespace identity is recoverable by sorting, which T16's line rewrite could ⊥ claim. DEPENDS on B10, which owns that incident: without its fix this MOVES the wrong sections. Its own row ∵ T16's is FULL at 1449 chars & the two proofs are ⊥ the same argument. Verify: a late §R moves & the file round-trips block-for-block · twice == once (V2) · a spec ALREADY in order is untouched|V1,V2,V11,V18
T19|x|`fmt` FOLDS an indented declaration back to column zero -- `  - V2:` → `- V2:`. WHITESPACE-ONLY ∴ V1's EXISTING proof covers it & no new one is needed, which is why this belongs in `fmt` & ⊥ in `migrate` (whose word-level proof is WEAKER, T16). Dedents ONLY id-shaped lines: promoting a genuine sub-point to top level would CHANGE MEANING, & 112 real sub-bullets across the fleet say that matters. WITHOUT it V28 stops the MERGE but the declaration stays INVISIBLE ∴ `check` still reports `cited but never declared` -- B8's wrong message, one layer down. MEASURED: 0 indented id-shaped lines in the fleet TODAY ∴ this is a tripwire BEFORE the growth, ⊥ a fire -- but markdownlint is installed fleet-wide & B8 proved it already rewrites these files. VERIFIED end to end: `  - V2:` folds to column zero & `check` then SEES the declaration, while `  - sub one` · `  - sub two` stay indented & stay SEPARATE. Twice == once|V1,V2,V18,V28
T20|x|`migrate` CONVERTS the declaration DIALECTS to canonical, in THREE shapes of one family: `| T1 | x | task | V1 |` → `T1|x|task|V1` · `| V1 | text |` → `V1: text` · `4. text` → `V4: text` in §V, where the ORDINAL IS THE ID -- FORMAT.md's `§S.n` says item 4 of §V IS §V.4 ∴ this READS the format rather than inventing a mapping. LOSSLESS by T16's word proof; `M` rows are EXCLUDED ∵ a milestone is rendered AS a table & converting it would destroy a legal shape. SCOPED: never inside a FENCE (B14), & never in a section that already declares canonically -- half-converted is worse than untouched, & 0 specs MIX. ⊥ blocked on the linter: that split lives at the HOOK layer, 31 of 51 already wire it ∴ REJECTED: nanokit writing an inline markdownlint directive -- it MEASURED well (98 → 0) but solves ABOVE the layer that owns this, & FORMAT.md could ⊥ take one without forking vendored bytes (V8). MEASURED over 51: VISIBLE task rows 821 → 2,047 · V13 37 → 8 ∴ all 29 dialect cases resolved & only REAL dangles remain. My PREDICTION (~37/51 passing) was WRONG, & HOW is the finding: passing went 24 → 23 ∵ 338 defects the parser could ⊥ SEE surfaced -- 279 V14 · 52 V25 · 7 V12 (B15). VERIFIED ⊥ my bug: rows convert at the SAME lines in the SAME order, & the worst spec already read T144 · T148 · T147. Exposing more than it fixes is the tool WORKING|V1,V2,V6,V18,V27
T21|.|FENCE awareness in `check` & `anchors`, sharing `format::is_fence` -- ONE definition (V7). B14 taught the FORMATTER that a fenced block is verbatim; the CHECKER was never told ∴ `## §B BUGS` inside a fence still reads as a real SECTION, & an id inside one reads as a DECLARATION. A spec that DOCUMENTS the format grows PHANTOM sections & phantom rules. MEASURED: 0 of 51 fleet specs put a fence inside §V/§T/§B ∴ this is a TRIPWIRE, ⊥ a fire -- said plainly rather than dressed up, ∵ B4's lesson is that an unmeasured rule is the defect. It earns its place anyway ∵ the alternative is TWO readings of `verbatim` in one crate, which is the shape B9 already cost us once. Verify: a fenced `## §B` is ⊥ a section (V18) · a fenced `V9:` is ⊥ a declaration · a REAL section after a fence still resolves|V7,V13,V18
T22|x|a CONSTRUCT fixture corpus, & the invariant it lands (V17 ∴ unnumbered here): a rule that reads MARKDOWN STRUCTURE ! be verified against every construct FORMAT.md permits, ⊥ only against our own spec. WHY: V29, which this task lands & which owns the argument. BUILT: 14 fixtures & 6 assertions -- `fmt` a NO-OP on each · idempotent on each · `check` SILENT on each · both halves of the bidirectional guard · the 3 merges planted. PROVED IT CAN FAIL, all 3 ways: a removed fixture FAILS · an unlisted file FAILS & NAMES itself · & REVERTING B13's one-line fix makes the corpus go RED -- the guard catches the bug the dogfood could ⊥ see, which is the whole claim. 2 fixtures cited invariants they did ⊥ declare & `check` said so while I wrote them|V7,V17,V18,V24
T23|.|`derive` reports the UNCLAIMED task range; it does ⊥ write a milestone. One line, only where tasks EXIST & `| M<n> |` rows do ⊥ -- which is exactly V15's blind spot BY DESIGN, since T15 scoped that rule to specs that OPTED IN ∴ this COMPLEMENTS V15 rather than restating it (V7), & where milestones DO exist V15 already names every unclaimed task. MEASURED: 33 of the 36 task-bearing fleet specs have NO milestone row & 913 todo tasks between them -- invisible until T20 made the rows readable at all (B15). MECHANICAL: the next free `M` id · the undone SET · the RANGE expression covering it, reusing `expand_cell`'s own grammar ∴ ⊥ a 2nd reading of it. JUDGEMENT, & left alone (V6): WHICH tasks form a milestone. Report-only, exit 0 (V10): the arithmetic is the tool's, the PARTITION is the author's. REJECTED: auto-writing a single `complete all tasks` milestone. All 3 cells ARE mechanical -- fixed scope, computed range, & a `done-when` that is a CHECKABLE predicate -- but it switches V15 ON & then TRIVIALLY satisfies it, turning honest SILENCE into meaningless GREEN. V15's own `claimed_twice` already names this: `EXACTLY one, or the rule is satisfied by claiming everything`. B15's shape one level up: a check green on a grouping that groups NOTHING. Verify: the line is PLANTED (V18) · SILENT where milestone rows exist · the range round-trips through `expand_cell`|V6,V7,V10,V15,V18

## §B BUGS

id|date|cause|fix
B1|2026-08-01|the gate's FIRST real run rejected the comment that exempted the vendored tree from it: the comment QUOTED upstream's misspelling as evidence, and `typos` scans `hk.pkl` too ∴ the exemption's justification became a new violation, one file further out. A rule that names its own trigger literally REINTRODUCES it|name the defect DESCRIPTIVELY (`a misspelling of "precedence" at Config.pkl:201`), ⊥ by quoting it, & say in the comment WHY it is not quoted -- otherwise the next reader restores the quote as a courtesy. Found by running the gate, ⊥ by reading it (V18)
B2|2026-08-01|INSERTING V4 (the pairwise rule) renumbered V4-V9 upward, and every citation IN THE CODE kept its old number ∴ 14 doc comments pointed one rule off -- the cap cited as the continuation rule, `ONE implementation` cited as `CPU only`. They still RESOLVE, so ⊥ a dangling-reference check can see them, & each reads as deliberate. Retiring old V10 (`nanokit DOGFOODS itself`) in the same edit orphaned its TWO runners: `tests/dogfood.rs` & hk's `nanokit` step enforced a rule with no text -- the inverse of V17|APPEND, ⊥ insert: a new rule takes the NEXT free number so no existing citation moves (V12 already says this for reuse; the same argument forbids insertion). Retargeted all 14 & restored the dogfood rule as V24. Found by AUDIT, ⊥ by a gate -- `check` (T4) enforces V13 over SPEC.md, & a citation living in a `.rs` doc comment is outside every checker this crate plans ∴ code citations stay a REVIEW obligation, named here so that is a known limit rather than an assumed cover
B3|2026-08-01|`check`'s FIRST run on this spec reported 3 dangling citations that were ⊥ citations: `grep V47` (an example command), `V99` (V13's OWN illustration of the failure) & `itok's V82` (a cross-project reference V19 explicitly blesses). V13 said every citation resolves & never said what a CITATION IS ∴ the checker counted every `V<n>` token, and the rule's own example was among the first things it rejected|DEFINE the boundary in the RULE, ⊥ in the checker -- V13 now states it & owns the reasoning. Read off FORMAT.md, which already reserves backticks for verbatim text: V7's defect arriving as two READINGS of one rule, ⊥ two copies of it. An allowlist per repo was REJECTED: it makes every example a local exception & leaves the rule undefined
B4|2026-08-01|T7 planned to DELETE the host checker & the consumer's driver on the belief nanokit had absorbed them. An audit BEFORE deleting found 5 capabilities it had ⊥ absorbed: a task-STATUS rule (missing outright) · a LINE number per violation · a stable `rule` id an agent matches on · the `Mechanical`\|`Judgment` fix taxonomy (an agent applying a Judgment fix BLINDLY silences the guard) · JSON output. `check` returns flat strings ∴ "absorbed" was read as RULE parity when it ! mean CAPABILITY parity, & the deletion would have dropped all 5 SILENTLY -- the exact drift this crate exists to end, in its own migration plan|MEASURE parity against the thing being replaced BEFORE planning its deletion, & write the comparison down. T9 now closes the gap; T7 waits. GENERALLY: "absorb" ! name a MEASUREMENT, ⊥ a belief -- a task that says `delete X` ! carry the evidence X is replaced, ∵ the deletion commit is exactly where nobody re-checks
B5|2026-08-01|`Fix::Mechanical` was written as "the action is EASY to perform" when the type exists to tell an agent what is safe UNATTENDED ∴ it ! mean "exactly ONE correct action & the tool can COMPUTE it". 5 of 8 directions were wrong & SHIPPED: V25's `set it to . ~ x` (WHICH? an agent taking it marks FINISHED work todo) · V12's `next FREE id` (renumbering redirects citations -- B2 again) · V13's `point it at the rule that was meant` (unknowable to a tool) · V15's `add it to a milestone` & `remove from all but one` (which one?)|RECLASSIFY to `Judgment`. `Mechanical` survives only where the tool computes the SINGLE answer: V11 add\|move a header · V14 move a row into id order · V15 drop a claim on a row that has no row. FOUND by DESIGNING `check --fix` -- asking "what may an agent apply blindly?" audits the labels that answer it, which reading them never did. ⊥ an invariant YET: the rule has no runner until `--fix` exists (V17)
B6|2026-08-01|`tests/dogfood.rs` measured the longest line in BYTES while `over_cap` enforces the cap in CHARS ∴ on a spec dense with `§` `∴` `⊥` (3 bytes each) the slack guard reported 36 fewer chars of headroom than existed & fired on a statement 216 under the cap. The guard answered a DIFFERENT question than the rule it guards|measure in the unit the RULE enforces. GENERALLY: a guard & its rule ! share a unit, or a green guard & a red one describe the same file. `format.rs` & `derive.rs` both chose chars DELIBERATELY with the reasoning written down; this test predated both & was never brought into line -- an older guard is the one to re-read when a newer decision is made
B7|2026-08-01|hk's `test` step was globbed `**/*.rs` while `tests/dogfood.rs` READS SPEC.md ∴ 4 spec-only commits skipped the ENTIRE suite & a slack breach (B6) landed unseen -- found only by a hand-run `hk check --all`, ⊥ by the hook that exists to find it. `coverage` had the same gap ∵ it runs the same suite|glob by DATA DEPENDENCY, ⊥ by file extension: a step's glob ! name every INPUT its tests read. Audited the siblings -- `clippy`, `doctest` & `rustdoc` genuinely read only Rust, so only `test` & `coverage` moved. B12's shape one level in: a rule that EXISTS & a gate that cannot reach it, green for the wrong reason
B8|2026-08-01|`check` reported 1,134 DANGLING CITATIONS across 51 fleet specs against invariants that ARE declared, often three lines above. `at_line_start` required the id at COLUMN ZERO, matching this repo & itok; 1,097 of 1,750 fleet declarations carry a markdown bullet. Same shape as B4 -- a grammar generalised from n=2 without measuring -- but WORSE IN KIND: B4's rule rejected legal specs, while this named the WRONG DEFECT ∴ a reader following the message hunts a declaration that was never missing. It also DROWNED the real findings: 1,134 of 1,147 violations were this one bug|V26 recognises the bullet. Canonical form stays column zero & is T17's rule with T16's migration ∴ recognising is ⊥ blessing. GENERALLY: a false POSITIVE naming the wrong rule costs more than a false negative -- it spends the reader's trust & buries the true rows. Surfaced only by RUNNING the corpus; no reading of this checker would have shown it. CAUSE of the bullets, & it changes T16: markdownlint DEFAULTS, propagated across repos ∴ this is ⊥ author drift but a TOOL's output, & a migration to column zero is one that tool REVERTS on its next run. A rewrite a linter undoes is ⊥ a migration ∴ T16 ! settle the linter question -- config, exemption, or canonical form accommodating the bullet -- BEFORE rewriting anything
B9|2026-08-01|T15 made `check` match a section header by its LETTER ∴ label-agnostic, & left `anchors` matching the `SECTIONS` strings WHOLE. Two readings of one thing -- the defect this crate exists to end -- regrown INSIDE it, one task later, by the commit that fixed the same class elsewhere. MEASURED on three fleet specs: of their section headers the old test recognised 0 of 6 · 0 of 12 · 0 of 6 ∴ `anchors` returned NOTHING addressable for any of them. SILENT: a listing verb that finds nothing looks like a spec with nothing IN it, & exits 0 either way (V10)|`opens_section` now CALLS `check::is_header_for` ∴ one definition, & the fix is an import. GENERALLY: when a shared notion is WIDENED, grep every READER of it in the same commit -- V7 is ⊥ satisfied by having written the helper, only by every caller USING it. A report verb hides this better than a gate does ∵ empty output is its success shape too
B10|2026-08-01|V11 walked sections in CANONICAL order comparing POSITIONS ∴ one section placed late made every section before it look EARLY. A fleet spec carries §R after §B -- ONE misplacement, reported as THREE, against §V · §T · §B, none of which had moved. Invisible until T10 taught `SECTIONS` about §R: an UNKNOWN section has no rank to be out of. THIRD of this shape in a day (B8, B9): right about WHETHER, wrong about WHICH|walk in READING order tracking the highest rank SEEN, & blame the section whose rank goes BACKWARDS. Why it is ⊥ cosmetic: T16's migrate READS this to decide what to MOVE ∴ it would have moved the wrong three & called the result canonical. GENERALLY: once a report becomes an INPUT to a mutation, `which` stops being message QUALITY & becomes CORRECTNESS -- audit every rule a `--fix` or `migrate` will consume BEFORE building the consumer
B11|2026-08-01|`migrate`'s losslessness proof used EXACT word equality while its own `residual` used CONTAINMENT ∴ two definitions of `the same word` in ONE module -- V7's defect at the smallest scale it has appeared. Consequence: `## §I INTERFACE` (SINGULAR, a real fleet header) canonicalises to `INTERFACES`, the token CHANGES & the proof called it a DROP, refusing a rewrite that discards NOTHING. Surfaced by running the CORPUS, ⊥ by 12 unit tests, ∵ every fixture I wrote used the plural I had in front of me. A 2nd bug in the same fn earlier: cutting the stem `interface` out of `Interfaces` leaves `s` ∴ every plural header earned a pointless note|a word SURVIVES if some output word CONTAINS it, & `residual` drops the whole TOKEN carrying the stem, ⊥ the stem's letters. GENERALLY: a PROOF & the TRANSFORM it guards ! share their notion of SAMENESS, or the guard refuses good work -- & no test written from the same head will show it. B6's unit mismatch (bytes vs chars) one level up; the CORPUS caught both
B12|2026-08-01|`fmt` MERGED any indented list item into the line above it: `carries_a_marker` tested the RAW line ∴ `  - V2:` & `  - a sub-point` both read as CONTINUATIONS. Two invariants became ONE LINE, & three bullets became one. V1's proof PASSED every time -- a merge is whitespace-only ∴ the losslessness guarantee is structurally BLIND to the exact failure this crate exists to prevent, a VANISHED INVARIANT. Then `check` reports `V2 is cited but never declared`, pointing at the line where V2 USED to be. MEASURED: 0 indented declarations in the fleet today, but 112 genuine sub-bullets ∴ adoption would have mangled all 112 SILENTLY, exit 0|V28: markers on the TRIMMED line, & the id set ⊇ before, asserted. GENERALLY: a proof states what it CHECKS, ⊥ what you HOPE -- `whitespace-normalised equality` was read as `nothing was lost` for 4 months. Ask of every proof: what TRANSFORMATION satisfies it while breaking the promise? Here the answer was one line of indentation
B13|2026-08-01|B12's fix taught `carries_a_marker` THREE BULLET CHARACTERS & stopped ∴ ORDERED list items still read as continuations: `1.` `2.` `3.` merged, & on a real fleet spec NINE invariants collapsed into ONE line. WORSE than the case it replaced, & by the same argument that justified V28: an ordered item is ⊥ a declared id ∴ V28's id-set proof cannot see it go EITHER, & BOTH proofs pass on a spec that lost 8 statements. Shipped INCOMPLETE & found minutes later, by a question about a DIFFERENT topic -- ⊥ by 144 tests, ∵ I tested the shape I had just fixed|accept `1. ` & `2) ` as markers, digits + `.`\|`)` + a SPACE ∴ a wrapped line merely BEGINNING with a number stays prose. GENERALLY: when a rule enumerates SYNTAX, the fix is ⊥ done until the enumeration is checked against the FORMAT, ⊥ against the example that failed. B12 & B13 are ONE defect reported twice ∵ I fixed the instance & called it the class
B14|2026-08-01|`fmt` MANGLED fenced code blocks. A `sh` block became ONE line -- three commands run together & the closing fence swallowed into the last of them -- ∵ a fence delimiter carries no MARKER ∴ every line inside read as a continuation. V1's proof PASSED, as it does for every merge. 7 of 51 fleet specs carry fences, 80 markers between them; ours carries ZERO ∴ the dogfood could ⊥ see it -- the 3rd such defect, & V29 now owns that lesson|a fenced region is VERBATIM: never joined, never folded, & wrapping RESUMES after it closes. This is V13's backtick boundary at BLOCK scale -- FORMAT.md already reserves backticks for verbatim text & that reading was load-bearing for CITATIONS; the formatter had simply never been told. GENERALLY: see V29
B15|2026-08-01|`check` reports CLEAN on specs whose ENTIRE §T it cannot see. 21 fleet specs write task rows as a MARKDOWN TABLE `| T1 | x | ... |`; those lines open with `|` ∴ `at_line_start` finds NO id ∴ 1,227 task rows are INVISIBLE, & V14 (order) · V15 (milestones) · V25 (status) silently check NOTHING. 16 of the 21 PASS. GREEN FOR THE WRONG REASON at fleet scale -- the exact shape B7 had in our own gate, & the `88 tasks in no milestone` failure §G was written about, recurring in the tool BUILT to catch it. It also inflated my own report: `28 of 51 passing` counted 16 vacuous passes|T20 CONVERTS the form ∴ the rows become visible & every rule reaches them. ⊥ a grammar loosening: `| T1 |` is a DIALECT of the canonical `T1|`, & recognising it in `at_line_start` would make `check` accept what `fmt` cannot produce (V2). GENERALLY: a PASS on a section the parser cannot SEE is indistinguishable from a pass on a correct one -- report what was EXAMINED, ⊥ only what failed
B16|2026-08-01|`render::human` emitted `SPEC.md:V13:22:` -- a BARE id, in the slot a COLUMN goes. Two defects in one line, both DELIBERATE & documented as intent (`the rule id where a column number would sit`). (a) V19 bound only what this spec CONTAINS ∴ said nothing about what it PRINTS, & the printed id lands in the CONSUMER's namespace: itok's V13/V14 are real, unrelated rules, & every id this crate emits collides there (100%, 11 of 11). Worst instance: itok's V14 is the invariant its nanokit migration REWRITES ∴ peak ambiguity exactly during the switch. (b) `file:line:col` is universal ∴ an editor jumping to field 2 lands on line 13 -- silent, plausible, wrong: a NEAR-collision, the expensive kind. Found by the first consumer READING the output, ⊥ by 130 tests, ∵ every test asserted the shape it was written against|rustc's shape: `SPEC.md:22: nanokit/V13: msg` -- a real line in the line slot, the id qualified & beside the message. `Display` drops the coordinates entirely (they are the CALLER's; `render` composes them) & carries `NAMESPACE` (one definition). V19 widened to cover OUTBOUND ids. json unchanged ∵ `rule` is a NAMED field. Guards: a PLANTED bare id asserted absent, & `SPEC.md:V` asserted never to appear
