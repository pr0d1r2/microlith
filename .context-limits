# RAISED a TENTH time, 16000 -> 18800, and the ninth's model held -- it was
# simply SILENT about one category.
#
# The ninth block sized this for the rate of MEASUREMENT: discovered work,
# bug records, what a run finds rather than what a plan lists. That reading
# is still right, and it is not what fired here. The release ladder --
# V30-V33, T24-T27, M9 and M10 -- is +1,474 tokens of PLANNED work, specced
# in ONE pass, none of it discovered. A file that grows at the rate of
# measurement ALSO takes a step function whenever a milestone is designed,
# and the ninth number left no room for one.
#
# So this is the first raise here that is neither creep nor discovery. It
# is a MILESTONE, which is what the FIRST raise called its own growth.
#
# 18,800 is ~12% over the measured 16,734, per V21's ceiling rule.
#
# THE STANDING PRECONDITION HELD, mechanically, twice: `nanokit derive |
# grep dup` reported NONE before the additions and NONE after. It did work
# in between -- the first run on the new rows found T24 restating V33's
# report-only clause, 10 words, written minutes earlier. Fifth consecutive
# find of the same pattern: a row written while the reasoning is fresh
# restates it instead of citing where it already lives.
#
# WHAT THE NEXT RAISE HAS TO ANSWER, since the trajectory is now
# 4,000 -> 4,500 -> 5,100 -> 5,800 -> 7,000 -> 8,100 -> 9,400 -> 11,500 ->
# 13,000 -> 16,000 -> 18,800:
#
#   The eventual remedy is upstream's and is still not available -- the ONE
#   FILE RULE triggers compaction at 500 LINES and SPEC.md is 131. But the
#   gap between 131 lines and this token count is V3's doing (one statement
#   per line), so waiting for 500 lines means waiting for a trigger this
#   file may never reach. If an ELEVENTH raise is needed, the question to
#   settle is not the number: it is whether the 500-line trigger is the
#   right one for a spec whose cost is measured in tokens.
#
# RAISED a NINTH time, 13000 -> 16000, and the SIZING MODEL changed with it.
#
# The eighth raise was an hour old. It was sized for the work on the LIST --
# B12, V28, T19, then T12-T14 and T18. What actually arrived was B13, B14 and
# T20-T22, none of which existed when the number was chosen: they came out of
# ONE question about a different topic, which surfaced two data-corrupting
# defects (ordered lists merged, fenced blocks merged) and three tasks.
#
# So the model was wrong, not the arithmetic. Every ceiling here has been
# sized for PLANNED work, and the growth has consistently come from
# DISCOVERED work -- 7 bug records today, ~1,750 tokens, none of them on any
# plan. A spec that records what measurement finds grows at the rate of
# MEASUREMENT, not at the rate of the task list. 16,000 is the first number
# here chosen for that rate.
#
# The mechanical test held: `nanokit derive | grep dup` reported none before
# this moved. That is now the standing precondition and it did its job.
#
# WHAT THIS CEILING IS FOR, since it diverges sharply from upstream's rule.
# FORMAT.md's ONE FILE RULE triggers compaction at 500 LINES. SPEC.md is 121
# -- 24% of it. Upstream would not have fired once all day. The gap is V3:
# one statement per line means 121 lines carry 13,671 tokens, so a line count
# says nothing about what this file costs to READ INTO CONTEXT, which is the
# only cost that matters for a spec an agent loads every session.
#
# That also names the eventual remedy, and it is upstream's rather than mine:
# at 500 lines, compact the OLDEST §B rows. 14 bug records currently cost
# 3,130 tokens. Not yet -- and when it comes it is a format-sanctioned edit
# with a diff, not a number quietly moved.

# RAISED an EIGHTH time, 11500 -> 13000, on the owner's decision.
#
# The note below pre-committed that an eighth firing would NOT be a raise --
# that it would mean the law had outgrown one file. It fired at 11,608, I
# declined to raise it unilaterally, and the owner ruled: raise.
#
# Recording that honestly, because the pre-commitment was mine and the call
# is theirs. Two things it did not anticipate, both of which weaken it:
#
#   1. FORMAT.md forbids the remedy the note assumed. "Too big to be one
#      file" is not an available move -- the ONE FILE RULE is upstream's, and
#      the format's own answer to size is compacting the OLDEST §B rows.
#      Trading bug records for bytes is exactly what V16 refuses.
#
#   2. The pass is no longer a ritual. T11 gave it a RUNNER, which is what
#      the fifth-raise note said the next raise should wait for. On its first
#      mechanical run it found FOUR duplications -- all written that same day,
#      all missed by reading -- and cutting them took the file from 11,608 to
#      11,529 without touching a single record. The number that fired was
#      partly duplication the ceiling itself surfaced.
#
# So the eighth raise buys the remaining specced work: B12, V28 and T19 (the
# fmt merge defect and its fix), plus T12-T14 and T18, which are mostly
# status flips and measurements. 13,000 is ~13% over 11,529.
#
# THE STANDING TEST IS NOW MECHANICAL, which changes what a future raise has
# to show. `nanokit derive | grep dup` must report NONE before the number
# moves. A raise while that says otherwise is a raise made to avoid an edit.

# SIZED ONCE, 9400 -> 11500, and this is meant to be the LAST raise.
#
# The seventh firing (9,466) came with the test from line 90 already
# written: "did the duplication pass find anything?" It found NOTHING --
# 0 pairs across every V/T/B statement, by the same script that found V5/V21
# and V13/B3. Three finds, then a clean sweep. The ritual has arrived.
#
# So the choice recorded above was taken, and the OTHER branch was taken:
#
#   COMPACT COMPLETED TASK ROWS -- rejected AGAIN, on a new ground. T1-T5,
#   T6, T9 are done and their rows carry ~795 tokens of design reasoning,
#   which is more than enough. But these commits were asked to be an AUDIT
#   TRAIL, and V19's "lineage lives in the commit trail" cuts BOTH ways: a
#   commit message is found by someone who already knows to look for it.
#   The row is what a reader meets first. Compacting the done rows would buy
#   795 tokens by making the spec worse at the one job the granular-commit
#   convention exists to serve.
#
#   TRIPWIRE, NOT BUDGET -- taken. A number that lands ~12% above current
#   seven times running is measuring the file. Sized instead for the spec
#   growth that REMAINS: T10 (§R), T15 (V11/V15 recalibration), T16
#   (migrate) and T17 (the canonical-label rule) each land an invariant plus
#   row amendments, ~400 tokens apiece on the measured rate. T11-T14, T7 and
#   T8 are already specced and cost only status flips.
#
#   9,466 + ~1,600 = ~11,100. 11,500 is that, rounded, with the slack a
#   tripwire needs to fire on SUDDEN growth rather than on the next task.
#
# WHAT THIS COSTS, said plainly: between here and 11,500 the ceiling will
# not refuse anything. It has not refused anything yet, so that is a change
# of honesty rather than of behaviour. If it fires an EIGHTH time, the
# reading is that the spec outgrew its own plan -- and the answer then is
# NOT a raise. It is that nanokit's law has become too big to be one file,
# which is a design finding, not a number to edit.

# Per-path token ceilings, set from COMMIT ONE (V9).
#
# This is the lesson nanokit exists to carry, transplanted as a number:
# itok set its first ceiling at 19,542 -- the size its SPEC.md ALREADY was
# -- and then raised it four times, to 27,500. A ceiling that arrives after
# the growth RATIFIES it; one that arrives first CATCHES it.
#
# RAISED A SIXTH TIME, 8100 -> 9400, when T12-T14 were specced and the
# ceiling fired at 8,415. The pass found two, both introduced MINUTES
# earlier in T13's own row -- the tightest that loop has closed yet, and
# three-for-three on the same pattern: a row written while the reasoning is
# fresh restates it instead of citing where it already lives.
#
# 9,400 is ~12% over the compacted 8,369.
#
# BUT READ THE TRAJECTORY BEFORE RAISING AGAIN:
#
#   4,000 -> 4,500 -> 5,100 -> 5,800 -> 7,000 -> 8,100 -> 9,400
#
# Six raises in one day; the spec has more than doubled. Every one followed
# the procedure and every pass found something, so the discipline is not
# failing -- but a ceiling that has never once REFUSED an addition is
# measuring growth rather than bounding it.
#
# There is a REJECTED OPTION whose trigger has now plausibly fired. When
# this fired the second time, "compact completed task rows" was considered
# and set aside: a done row's detail also lives in its commit message, and
# V19 says lineage lives in the commit trail. It was rejected then because
# the pass had cheaper duplication to cut first. It no longer does -- T4,
# T6, T9 and T10 are done, and their rows carry full design reasoning that
# the commits already hold.
#
# So the honest choice at the SEVENTH raise is not 12% again. It is either
# that compaction, or admitting the ceiling is a tripwire for sudden growth
# rather than a budget, and sizing it once for the remaining tasks.
#
# RAISED A FIFTH TIME, 7000 -> 8100, when T11 was specced and the ceiling
# fired at 7,235.
#
# THE TEST PASSED AGAIN, and more sharply than last time: the duplication
# pass found V13 and B3 sharing 11 words -- in a spec deduplicated HOURS
# earlier, by the same technique, looking for exactly this. Reading had
# walked past it twice that day.
#
# The pattern behind both finds is now named: a RULE and the BUG RECORD
# that produced it each restate the justification, because writing the
# record while the incident is fresh means writing the reasoning again. The
# rule owns it; the record cites it.
#
# 8,100 is ~12% over the compacted 7,216, per V21.
#
# T11 will give this pass a RUNNER. Until it lands, every raise below
# depends on someone remembering to run a throwaway script -- which is why
# T11 exists at all, and why the next raise should be the first one where
# the pass is mechanical rather than remembered.
#
# RAISED A FOURTH TIME, 5800 -> 7000, when T10 specced the 4.1.0 sync and
# the ceiling fired at 6,261.
#
# THE TEST BELOW WAS APPLIED, and it passed: the duplication pass FOUND
# something. V5 and V21 had carried the same fourteen words verbatim since
# V21 was written that morning, and three earlier compaction passes had all
# missed it -- because a repeated clause reads perfectly well in both places.
# Found by MEASURING this time: a script over every V/T/B statement looking
# for word-sequences appearing in more than one.
#
# So the ceiling is still doing work rather than performing it. 7,000 is
# ~12% over the compacted 6,201, per V21.
#
# The LINE cap earned its keep in the same pass, and better: it rejected
# T10's row at 1722 chars, and the right fix was not compression. The row
# specified behaviour for `add R`, a verb that is neither built nor specced.
# A length limit caught a FORWARD REFERENCE. That is the second time a
# threshold here has found a defect rather than merely bounded a number.
#
# RAISED A THIRD TIME, 5100 -> 5800, when T9 and B4 were specced and the
# ceiling fired at 5,187. Procedure as recorded below; the compaction pass
# found only 20 tokens this time, because T9's row was already dense.
#
# THE PATTERN IS NOW THE INTERESTING PART, so read it before raising a fourth
# time. 4,000 -> 4,500 -> 5,100 -> 5,800, each about +12%, all in one day.
# The ceiling has never REFUSED an addition; it has only ever delayed one by
# the length of a duplication pass.
#
# That is not nothing -- the pass found real duplication twice, and each raise
# forced its reasoning into a commit rather than a reflex. But a ceiling that
# always ends up ~12% above current is TRACKING the file, not binding it, and
# a reader should know that is what this number now does.
#
# The honest test for a fourth raise: did the duplication pass find anything?
# If it keeps finding nothing, the ceiling has become a ritual, and the
# decision to make is whether to size it for the remaining tasks in one go or
# to admit it is a tripwire for SUDDEN growth rather than a budget.
#
# RAISED TWICE IN ONE DAY. 4500 -> 5100 when T4 landed and the ceiling fired
# again at 4,587. Same procedure as below, in the same order: cut duplication
# first (V13 and §I each restated B3's record; -58), re-measure (4,529), then
# size by V21's rule -- 5,100 is ~12.6% over.
#
# The note below predicted this and said it would be worth knowing. It is:
# T4 cost +484 tokens MEASURED, and T5, T7, T8 remain, so on this trajectory
# 5,100 buys about one more task. That is the deliberate choice -- a ceiling
# sized to the trajectory (6,000 was the alternative, and was rejected) would
# stop catching anything in the meantime, which is what a ceiling is for.
# Expect a third raise, and expect it to be reviewed the same way.
#
# What did NOT happen, and is the point: no rejected-option record was traded
# for bytes (V16), and no threshold moved before the duplication pass.
#
# RAISED ONCE, 4k -> 4500, on the day M2 landed. The ceiling FIRED at 4,105
# against 4,000 -- which is the whole point of setting it at 2x the size the
# file then was, rather than at the size it already had.
#
# The review it triggered, in order, because the order is the discipline:
#
#   1. Look for duplication first. Found some: T6 and V24 restated B1's and
#      B2's records, written before those rows existed. Cut, -60 tokens.
#   2. Re-measure. 4,045 -- still over. What remained was prose, not
#      restatement, and a spec compacted to satisfy a number trains the
#      same reflex as a ceiling raised to satisfy one.
#   3. Only then size the new number BY RULE, not by what fits: V21 says a
#      ceiling wants ~12% headroom, and 4,500 is 11% over 4,045.
#
# The growth was a MILESTONE, not creep: V24, B1, B2 and a rewritten T6,
# each carrying a runner or evidence, and V16 forbids trading a
# rejected-option record for bytes. Four tasks remain, so this will be
# tested again -- and a second raise inside a task or two would be evidence
# that 4,500 was also chosen too small, which is worth knowing.
#
# Checked by `itok check` (`--bpe`, so the verdict is deterministic). T4 has
# landed and did NOT take this over: `nanokit check` reads SPEC.md's structure,
# not its size. T5's `derive` reports sizes; whether the GATE moves here is a
# decision for T5, not an assumption to leave standing in a comment.
SPEC.md    18800
