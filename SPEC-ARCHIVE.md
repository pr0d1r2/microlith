# SPEC ARCHIVE

Task rows moved out of `SPEC.md` by `mth archive`. An id is never reused
(`V12`), so a citation to an archived row still resolves -- here.

This is a SINK, not a spec. The citations inside these rows point into
`SPEC.md`, so `mth check` on this file reports every one of them as dangling,
correctly and uselessly. The verb that reads it is `mth tasks`.

## §T TASKS

T1|x|`fmt` core: unwrap hard wraps, pairwise continuation rule, line cap; lossless proof asserted BEFORE the write; idempotence tested|V1,V2,V3,V4,V5
T2|x|`fmt --check`: report drift & exit 1, ⊥ rewrite; the `rustfmt`/`nixfmt` grammar so it needs no teaching|V10,V2
T3|x|`.context-limits` at 4k from commit one + `tests/dogfood.rs`: this SPEC.md is formatted, under the cap, and carries real slack -- asserted against the library being built|V9
T4|x|`check` verb, RUN & green: V11-V16 as pure fns over `&str` in `src/check.rs`, each with a PLANTED violation & a companion accepting every real shape; `mth check --records .spec-records SPEC.md` exit 0, gated by hk & by `tests/dogfood.rs` (V24). Rules restated HERE, ⊥ referenced, ∴ deleting the consumer's copy in T7 costs no evidence (V19). `src/id.rs` holds the id grammar ONCE ∵ `fmt` & `check` both need it & two parsers would BE the defect this crate ends (V7). V16 takes the baseline from the CALLER (`--records`) ∵ survival across edits is ⊥ derivable from one file. MEASURED: coverage ROSE 94.68→97.12% ∵ the arg handling arrived WITH its tests. First real run found 3 false positives ⇒ B3 & V13's backtick boundary|V7,V11,V12,V13,V14,V15,V16,V18
T5|x|`derive` + `anchors`, RUN & green: sizes · citation counts · orphans · `§S.n` addressing, all report-only ∴ exit 0 EVEN WITH FINDINGS -- an orphan is a question for a reader, ⊥ a build failure, and a gate would answer it by fiat (V10). Sizes are CHARS: ⊥ bytes (this format is dense with 3-byte symbols) & ⊥ tokens (a tokenizer is a dependency; `itok` owns that, ⊥ us). `anchors` emits FORMAT.md's ORDINAL with the id it resolves to BESIDE it, ∵ an ordinal SHIFTS when an earlier id retires -- B2 one level out -- so drift is SHOWN, ⊥ designed around; a MILESTONE row is furniture ∵ counting it would push every `§T.n` off its task id on a WELL-FORMED spec. FOUND on first run: V8 orphan (uncited) · §B rows cost 809 chars mean vs §V's 389 ∴ a bug record outweighs the rule it protects|V6,V10,V18
T7a|x|`docs` verb + the FREEZE guard: ONE registry (verb → synopsis · flags · exit) renders `--help` AND the markdown reference, & a gate step diffs it against the committed README block ∴ a stale README is a RED gate, ⊥ a chore nobody owns. Landed as a TEST, ⊥ an hk step of its own: hk already runs `cargo test` ∴ a second copy in `hk.pkl` would rest the verdict on the runner's own logic, which V22 forbids. The registry lives in the BINARY ∵ everything the lib exports is a promise (V32) & nobody calling the rules needs help text. One blurb serves BOTH renderings ∵ `--help` WRAPS at render time -- a hand-folded literal can only give the terse one. FOUND by building it: the drift was ⊥ hypothetical & ⊥ only the one already recorded -- `--help` never mentioned `derive`'s duplication report (T11, done), a SECOND stale copy nobody had noticed. VERIFIED both ways (V18): a planted registry row diverges from the committed block, & a hand-edit to README makes the freeze RED live|V33,V10,V18,V22,V32
T7b|x|`CHANGELOG.md` (Keep a Changelog + SemVer) carrying V30's ladder as a TABLE, & `CONTRIBUTING.md` for the spec-first loop. WHY OUTSIDE SPEC.md: a consumer arriving from crates.io never opens our law ∴ a ladder that lives only in §V is one only WE can read, & the number they see would have no stated meaning. LANDED: the table renders all NINE rungs with V34's parity as its own column ∴ the even/odd claim is visible WITHOUT reading §V, & `0.1`-`0.4` are marked REACHED-⊥-PUBLISHED rather than omitted, ∵ a ladder that hides its unpublished rungs makes `0.5` look like a first version instead of a fifth. CONTRIBUTING ⊥ restates AGENTS.md (V7): it is the ARRIVAL path -- setup, the loop, the one hard rule, what gets a patch turned down -- & DEFERS to AGENTS.md for depth. Both SHIP in the `.crate`, which is the point of T7b: they are for the consumer, ⊥ the developer|V7,V30,V34
T7c|x|`CODE_OF_CONDUCT.md` & `SECURITY.md` -- the two files a STRANGER looks for before opening an issue, & neither derivable from the code. WRITTEN, ⊥ vendored: the Contributor Covenant 2.1 is ADAPTED so this repo's own standard is IN the document -- a claim carries EVIDENCE, & that binds maintainers exactly as it binds a contributor ∴ pushing back on an unsupported claim is PARTICIPATION. SECURITY states the surface HONESTLY rather than performing rigour: zero deps · no network · no `unsafe` (forbidden, ⊥ merely absent) · ⊥ code execution ∴ the REAL classes are THREE -- a write that loses content (V1's proof having a gap, & the one that gets priority ∵ it is the user's FILE), a panic or hang on hostile input, & a check that passes on a file it could ⊥ PARSE, which is B15's shape & the defect this crate EXISTS to prevent. `FORMAT.md` findings are OUT of scope & routed upstream (V8): vendored bytes are ⊥ ours to fix|V1,V8
T7d|x|`INTEGRATION.md` -- the GUARDRAILS as a process a human can follow: what runs at commit · at push · on CI, which FILES each stage examines, & why the cargo steps are CHAINED. WHY ITS OWN FILE: the gate was described in FIVE places & drawn in NONE ∴ a reader asking what stops an LLM shipping garbage had to RECONSTRUCT it. AGENTS.md's gate section now DEFERS here rather than restating (V7). DIAGRAMS ARE ASCII, ⊥ mermaid: two syntax reductions failed to render on the instance this repo is inspected from ∴ a diagram is a CLAIM & a claim nobody can READ is worse than none. Records the `--pr` DECISION that was invisible before: CI runs `--all`, ∵ a changed-files scope cannot see damage to files nobody TOUCHED|V7,V22
T7e|x|TRIM the lib's pub API to the VERBS before `0.5` ships (V32). MEASURED before: 80 pub items across 8 pub MODULES, & the binary referenced 23 of them ∴ 54 were public for NEITHER a consumer NOR the CLI -- they are the parser, reachable `pub(crate)`. MEASURED after: 20 items, ZERO pub modules. THE CAUSE was structural, ⊥ sloppiness: `src/main.rs` is a SEPARATE crate ∴ it sees only `pub`, & it imported 7 of the 8 modules -- the front door was open because the BINARY walked through it. FIXED by moving the CLI INTO the lib as `run`, leaving main.rs 16 lines: argv in, streams & exit code out. README claimed `the binary is a thin shell` for months while it drove 7 modules; that sentence is now TRUE. PARITY is the point & the old surface FAILED it BOTH ways -- too much plumbing (54 internals) & too little porcelain (2 verb-level fns for 6 verbs) ∴ `migrate` · `derive` · `anchors` · `docs` could ⊥ be called at all without assembling module internals. Now ONE fn per verb + `run` for the whole CLI|V7,V32
T8a|x|`no-commit-to-branch` on `main`, & ⊥ BEFORE the release. hk ships the check; it is UNUSED here ∵ every commit to date lands on `main` DIRECTLY ∴ arming it today means bypassing it on its FIRST run, & a guard whose first lesson is the bypass is worse than none (AGENTS.md's ONE RULE). The TRIGGER is the workflow CHANGING -- ⊥ the moment somebody writes the step -- & it FIRED EARLY, at the FORGE rather than at `0.5.0`: branch protection on `main` now REQUIRES a pull request & all 3 `gate` jobs, with admins ⊥ exempt ∴ the bypass this row refused to teach is ⊥ AVAILABLE to teach, & arming the local half became consistent rather than premature. PROVEN: a direct push is `GH006 ... Changes must be made through a pull request`. PAIRED with the forge: V23 makes every hook here skip loudly OUTSIDE the dev shell ∴ a local rule is ADVICE & the branch protection ! also exist SERVER-SIDE; the hook's job is to fail EARLY & readably, ⊥ to be the only defence. SCOPED to the pre-commit hook, ⊥ `fast`: `all` IS `fast` + 4 & CI runs `hk check --all` ON `main` after every merge ∴ a branch guard in `fast` fails every CI run on the branch it protects. Verify: a commit on `main` is REFUSED & the same commit on a branch is ⊥ (V18)|V17,V18,V23
T9|x|PARITY with the host checker, MEASURED ⊥ believed (B4's own rule): all 5 gaps closed & re-compared -- task status (V25) · `line` · stable `rule` id · `Mechanical`\|`Judgment` · JSON. A `Violation` carries the failure ANATOMY: `rule` (match on THIS; prose is ⊥ stable) · 1-based `line`, 0 = document-scoped · `why` in one clause · RANKED `directions`, correct fix FIRST. V16 offers NO mechanical direction BY DESIGN ∴ an agent hitting it must STOP -- which is what trading a record for bytes ought to feel like. `--format json` renders the SAME anatomy, hand-encoded ∵ zero deps ∴ escaping is TESTED (quote · backslash · control · newline), validated through a real parser. Unknown format = USAGE error, ⊥ silent prose. CAUGHT while wiring: `positional` skipped one flag's value ∴ `--records r --format json SPEC.md` would have taken `json` as the path|V25,V10,V18,V7,V17
T11|x|`dup`: cross-statement DUPLICATION, as a section of `derive`. WHY: `.context-limits` names "the duplication pass" 4× as the gate on every ceiling raise & it has NO RUNNER -- it is run BY HAND ∴ a procedure this repo already relies on is V17's lie that reads as law. MEASURED twice: V5/V21 shared 14 words (missed by 3 read-throughs ∵ a repeated clause reads fine in both places) · V13/B3 share 11 in a spec deduped HOURS earlier. Threshold 8 words, MEASURED ⊥ chosen (V5): 5-6 admit coincidences · 7-10 yield ONLY the genuine hit · 12 misses it. NO knob -- a threshold with a flag is one someone lowers to make a report quiet. ⊥ a new verb: `derive` already answers how-big · what's-biggest · what's-uncited, & this is what's-said-TWICE. ⊥ a `check` rule ∵ duplication is a SMELL, ⊥ a violation -- a spec ? legitimately repeat itself. Report-only, exit 0 (V10): names BOTH ids, recommends NEITHER ∵ which copy dies is JUDGEMENT. Strips backticked spans, REUSING V13's boundary ∴ a shared command name is ⊥ shared reasoning; case-INSENSITIVE & MAXIMAL phrases only -- both were bugs in the hand-run script. DOGFOOD (V24): our own spec carries NONE ≥8 ∴ V13/B3 is cut in the SAME commit or the guard is RED AT BIRTH (V17). RED AT BIRTH as predicted: 1st run on our own spec found 4 pairs, all written TODAY -- T18/B10 (10 & 9 words) · T15/V11 · V27/V11 (9). Cut HERE ∴ green, & the 1st found by a RUNNER ⊥ by hand|V3,V5,V6,V10,V13,V17,V18,V24
T15|x|CALIBRATE the rules against the CORPUS. Rules derived from n=2 (this repo & itok) & generalised without measuring ∴ 2 of 51 fleet specs passed. RECORDED, ∵ the number is the calibration's only honest measure: **2 -> 35 pass** (28 non-vacuous; 7 have no items ∴ pass by having nothing to check) & **1,147 -> 51 violations**. THREE miscalibrations, each a TRUE rule with a FALSE message: V11 demanded all SIX sections PRESENT (only 41 of 51 have §G) & matched the header STRING (only 16 spell `## §V INVARIANTS` exactly, 9 `Invariants`, 7 `— Invariants`); V15 fired on EVERY task in the 48 specs with no MILESTONE row; & the id grammar read COLUMN ZERO only, ∴ 1,134 bogus dangling citations (B8). The 51 REMAINING: 22 REAL defects (9 V11 · 5 V14 · 8 V13) & 29 a NAMED EXCEPTION -- two DECLARATION DIALECTS, a markdown TABLE row (20) & a plain NUMBERED LIST (9), both mechanically migratable ∴ T16's, ⊥ a rule to loosen. Also found & left to T16: `## S.G` ASCII for `## §G`. EXTENSION sections tolerated: §D · §E · §F · §O · §P · §X|V11,V15,V18,V24,V26
T16|x|`migrate`: normalise section HEADERS to canonical 4.1.0, & ⊥ one byte dropped. THREE cases, & V27's audited `SYNONYMS` table decides WHICH ∴ the judgement is DATA, ⊥ inference: (1) case & punctuation only (`— Interfaces` · `Interfaces`) → rewrite SILENT, nothing to preserve; (2) BARE `## §V` or a LISTED synonym → canonical header & the original kept beneath as `Migrated by microlith from: <original>`; (3) a COLLISION (`§V VERSIONING`) → ⊥ TOUCHED, reported. SUPERSEDES this row's 1st draft (report all 13, touch none): V27 made the split MECHANICAL. WHY (3) stays untouched: annotating a collision keeps every char & INVERTS the meaning -- a build section under a BUGS header -- lossless in BYTES, wrong in SUBSTANCE, & it PASSES the byte proof. PROOF, asserted BEFORE the write: every ALPHANUMERIC run of the original survives, CASE-INSENSITIVELY, in the canonical label or the note. ⊥ V1's whitespace identity -- migrate CHANGES text ∴ needs its own, & case-folding is what makes (1) provable. `## §T55-PLAN` is ⊥ a header ∴ letter-then-DIGIT rejected. Idempotent (V2): a 2nd run sees canonical, does nothing, adds no 2nd note. DECLARATION bullets ⊥ migrated -- B8's linter question unsettled ∴ a rewrite markdownlint REVERTS. MEASURED over 51: 26 REWRITTEN · 14 NOTES · 6 DECLINED · 0 refusals · 0 changed on a 2nd run (V2) · V27 31 -> 18, & the 18 are EXACTLY the collisions ∴ it fixed all it claimed, none it disclaimed|V1,V2,V6,V18,V27
T17|x|A CANONICAL LETTER CARRIES ITS CANONICAL MEANING -- landed as V27 with its checker (V17). MEASURED over the fleet: 31 violations, ⊥ the 25 predicted -- the extra 6 are BARE headers (`## §V`, no label at all) that the earlier regex skipped ∴ the prediction was low by exactly the shape it could ⊥ see. The SPLIT came out as designed & is where the judgement lives: 13 MECHANICAL (7 synonyms + 6 bare) · 18 JUDGMENT = 6 repos × 3 collided sections. Low-noise as argued: 236 of 261 headers already carry the word, & `— Bugs / Known Issues` · `— Invariants (rules research must hold)` · `Research — gem/tooling evaluation` all PASS. The synonym table got a RUNNER TODAY rather than waiting for T16: it picks the fix KIND ∴ ⊥ a rule with no runner (V17). A synonym is repairable -- canonical word + the old label kept as a note, lossless; a collision is ⊥, ∵ the CONTENT ! move & no header rewrite moves it|V11,V17,V18,V24,V26
T19|x|`fmt` FOLDS an indented declaration back to column zero -- `  - V2:` → `- V2:`. WHITESPACE-ONLY ∴ V1's EXISTING proof covers it & no new one is needed, which is why this belongs in `fmt` & ⊥ in `migrate` (whose word-level proof is WEAKER, T16). Dedents ONLY id-shaped lines: promoting a genuine sub-point to top level would CHANGE MEANING, & 112 real sub-bullets across the fleet say that matters. WITHOUT it V28 stops the MERGE but the declaration stays INVISIBLE ∴ `check` still reports `cited but never declared` -- B8's wrong message, one layer down. MEASURED: 0 indented id-shaped lines in the fleet TODAY ∴ this is a tripwire BEFORE the growth, ⊥ a fire -- but markdownlint is installed fleet-wide & B8 proved it already rewrites these files. VERIFIED end to end: `  - V2:` folds to column zero & `check` then SEES the declaration, while `  - sub one` · `  - sub two` stay indented & stay SEPARATE. Twice == once|V1,V2,V18,V28
T22|x|a CONSTRUCT fixture corpus, & the invariant it lands (V17 ∴ unnumbered here): a rule that reads MARKDOWN STRUCTURE ! be verified against every construct FORMAT.md permits, ⊥ only against our own spec. WHY: V29, which this task lands & which owns the argument. BUILT: 14 fixtures & 6 assertions -- `fmt` a NO-OP on each · idempotent on each · `check` SILENT on each · both halves of the bidirectional guard · the 3 merges planted. PROVED IT CAN FAIL, all 3 ways: a removed fixture FAILS · an unlisted file FAILS & NAMES itself · & REVERTING B13's one-line fix makes the corpus go RED -- the guard catches the bug the dogfood could ⊥ see, which is the whole claim. 2 fixtures cited invariants they did ⊥ declare & `check` said so while I wrote them|V7,V17,V18,V24
T26a|x|`§F`/`§N` become KNOWN letters, checker in the SAME commit (V17): `SECTIONS` 7→9 · `KINDS` gains `F`·`N` · `CANONICAL_WORDS` gains `federation`·`nav` · V11's rank places both between `§G` & `§C`. NO id grammar change: an `§F` row carries NO id ∴ V12 & V14 have nothing to make unique or to sort, & inventing one would be a rule with no defect behind it. VERIFIED by PLANTING (V18), & PROVED it can FAIL both ways: ranking `F`·`N` at the END of `KINDS` turns 3 tests RED, & dropping the 2 words from `CANONICAL_WORDS` turns the COLLISION test red. `§F` after `§C` is REJECTED & BLAMED BY NAME · `## §F Federation` · `## §F — Federation` · `## §N Navigation` all PASS ∵ V27 matches a STEM · `## §F FIXTURES` is a JUDGEMENT refusal, ⊥ a rewrite · a spec with NEITHER section passes UNTOUCHED -- that companion is the one that matters here, ∵ this rule's failure mode is LOUDNESS on repos that declined an optional feature, which is V15's own history. 2 FIXTURES per V29, ∵ `§N` WITHOUT `§F` is the LEAF case & neither section implies the other. The SWEEP is STORED as `examples/corpus.rs` ⊥ retyped: 3 runs of one measurement, the first 2 thrown away, & the one that MATTERED corrected V39's headline (V7 at INSTRUMENT scale). DELTA, 256 specs: clean 114 → 114 · violations 222 → 224, all on 1 already-red spec -- measured with V42 OFF; the sweep says 107 today (B33). MISSED a 4th home for the section set (B25: `anchors` addressed neither letter)|V11,V17,V18,V27,V29,V39
T26c|x|`FORMAT-EXTENSIONS.md` & its RUNNER in ONE commit (V17), per V40. WHAT IT IS FOR, & it is ⊥ this repo: a cavekit user with no microlith can ADOPT `§F`/`§N` by READING one file -- the extensions travel WITHOUT the tool, which is what makes this a SUPERSET rather than a dialect only we can parse. RENDERED from `SECTIONS` + `CANONICAL_WORDS` (V7: the letters & their rank have ONE home & it is the one `check` reads) + a REGISTRY carrying only what a `const` cannot derive -- what each section HOLDS & an EXAMPLE. `UPSTREAM_KINDS` names FORMAT.md's 7 at the PINNED rev ∴ EXTENSION = `SECTIONS` MINUS upstream, COMPUTED ⊥ listed. VERB `mth extensions`, report-only to stdout (V10, V33): the tool must PRODUCE the file or a reader cannot regenerate it, & the redirect stays the USER's. VERIFY: the committed file IS the render (freeze) · a registry entry with no letter FAILS & a letter with no entry FAILS (bidirectional, T22's shape) · PLANT a drifted registry & the freeze goes RED, or it is indistinguishable from one that cannot fail (V18). SHIPS in the `.crate` & is NAMED in `must-package` ∵ a document a stranger adopts is worthless if the tarball drops it. FOUND landing it: the `--help` VERB LIST was never WRAPPED & the 8th verb pushed it to 83 cols, past the 76 its OWN test holds every other line to -- a list that GROWS ! ⊥ be left unwrapped. Fixed here|V7,V8,V10,V17,V18,V32,V33,V40
T26e|x|the DOCUMENT carries its EVIDENCE. V40 published WHAT the extensions are & said NOTHING about who uses them or what claiming a letter COSTS -- the 1st question a format's maintainer asks, & the document is written to travel to someone with NO access to the sweep. ∴ `measured` per entry, rendered as a `*Measured:*` line, + the DENOMINATOR & DATE stated ONCE: 256 distinct specs · 62 projects · 2026-08-28 · clean 114 → 114 for the LETTERS, & 107 at this build (B33). COUNTS travel & NAMES do ⊥ (§C: the corpus is PRIVATE). FROZEN prose, ⊥ a derived number, & HONEST about it -- the gate CANNOT re-run a sweep over a corpus a consumer has ⊥ got, so it carries a DATE like every other measurement V33 freezes. GUARD: every entry ! carry one & the render ! emit exactly as many `*Measured:*` lines as there are entries ∴ an entry added later WITHOUT its number ⊥ READS as though the same care was taken. WHY IT EARNS A ROW: V39's 1st count was WRONG at a denominator it never STATED, & the correction cost 1 run of a tool that now exists -- a proposal without a denominator is an OPINION|V33,V39,V40
T31|x|FORMAT.md's ANCESTRY gets a RUNNER. §C called this file NORMATIVE & NOTHING recorded WHICH upstream version it matched ∴ upstream MOVED -- gaining `§R` · sectioned ownership · right-size -- & this repo learned it by being ASKED, which is V17 aimed at the spec that DEFINES V17. SPLIT by whether a check needs the NETWORK, ∵ the gate REFUSES one (the `links` precedent): OFFLINE, `format-upstream` in `hk.pkl` asserts FORMAT.md still hashes to `.format-upstream`'s record ∴ a format edit lands WITH its record. ONLINE, a WEEKLY workflow compares upstream's CURRENT copy & fails ITS OWN run, ⊥ a pull request -- whether upstream moved has NOTHING to do with the change under review, & a check that fails unrelated PRs is one people learn to CLICK PAST. Each half NAMES which half it is (V36). RED until the record is updated ∴ self-clearing, NO issue to close. MEASURED at landing: this copy ≡ upstream `c322f0b`. PROVEN by PLANTING: an edited FORMAT.md exits 1 & names the recorded hash · an EMPTY record exits 1 · restored exits 0|V7,V17,V18,V36
T33|x|the sweep exercises the WRITING verbs, & needs NO copy of a foreign repo to do it. `check` alone was swept -- the HALF that never had the expensive bugs: B12·B13·B14 were ALL `fmt` merging a construct our own spec lacks, ALL found by the corpus, ALL invisible to the dogfood. READ-ONLY BY CONSTRUCTION, ⊥ by care: `format_spec`·`migrate_spec` are `&str -> String` & EVERY write in this crate lives in `cli.rs` ∴ 614 foreign spec FILES (256 distinct) are read & ⊥ TOUCHED, & the tmp-copy question answers itself. The REFUSAL is SPLIT by CAUSE ∵ lumping them hides the one that matters: OVER THE CAP is EXPECTED (no other project opted into our limit) & a LOSSLESSNESS refusal is V1 FAILING on a real file. MEASURED 2026-08-28 over 256 DISTINCT specs (the denominator B27·B28 corrected): 99 would be REWRITTEN · 30 over cap · 7 migrate DECLINED (collisions, correctly untouched) · **0 lossless-proof refusals & 0 UNSTABLE** ∴ V1 & V2 hold at CORPUS scale, ⊥ merely on 17 fixtures -- which is the claim `0.6`'s README makes & nothing had checked at this size|V1,V2,V6,V24,V29
T36|x|COVER the 5 lines the library CANNOT reach. `main.rs` sat at 0% ∵ every rule & verb is tested through `run()`, which returns a STRUCT & touches no process -- that is the DESIGN (V7) & why the suite is fast. But what lives there is ⊥ decoration: `ExitCode::from(o.code)` IS the gate contract -- `check` exits 1 on drift & every hook, CI job & `hk` step downstream BELIEVES that number. Drop it, route `out` to STDERR, or forget `.skip(1)` over argv, & the whole library suite stays GREEN while the binary is broken in the ONE way that matters: a gate that CANNOT FAIL, which is worse than no gate ∵ everything downstream reports success. ∴ the 1 place a PROCESS is worth spawning, & `CARGO_BIN_EXE_mth` is cargo's OWN path to it ∴ no dep (§C). PLANTED both defects (V18): dropping the exit code turns 2 RED, swapping the streams turns 3 RED. 5 assertions -- exit 1 on drift · 0 on clean, or the 1st is satisfied by a binary that ALWAYS fails · 2 on usage, the distinction a caller SCRIPTS against · report to STDOUT so `mth docs >` works · & the verb READ at all, which is `.skip(1)` proving itself|V7,V10,V18,V24
T37|x|the SWEEP gets TESTS & a RUNNER. `examples/corpus.rs` is 530 lines producing EVERY measurement this repo publishes, & it carried NO tests -- it was wrong 4 times (B27·B28·B31·B33) & each wrong number was PUBLISHED before anyone noticed. NAMED as debt in #12's own body ∴ ⊥ discovered, DEFERRED. 12 tests, each pinning a bug that HAPPENED ⊥ a function that exists: the 3 snapshot shapes `is_copy` missed & the COMPANION that an ordinary name is ⊥ a copy · identical text counted ONCE · a project credited ONLY for a spec that SURVIVED · a rule broken TWICE in 1 spec = 2 findings & 1 SPEC · a cap refusal ⊥ a proof refusal. RUNNER, & this is the half that nearly repeated the defect: `cargo nextest run` does ⊥ run tests inside an EXAMPLE -- it lists 5 binaries & `corpus` is ⊥ among them ∴ the suite would have had NO runner (V17), 1 layer out from the rules. Its OWN hk step, ⊥ moving the code to `src/`: the sweep reads a developer's disk & a consumer has no fleet (§C excludes `examples/`). The gate's step-count guard CAUGHT the stale doc immediately|V7,V17,V18,V24
T38|x|the CEILING gets a RUNNER. `.context-limits` named a budget for SPEC.md from the START & NOTHING here enforced it -- the file SAYS so (`STILL UNENFORCED. itok owns the checker`) & was RAISED 11 times on a number measured BY HAND from a sibling checkout. MEASURED the day a runner existed: 32,513 o200k vs a 23,200 ceiling, ~40% OVER, & over for some time. ⊥ COUNTED HERE: BPE needs a TOKENIZER -- a table or a dep, in a crate that promises NEITHER (§C) -- ∴ the answer was always to CALL the owner (V7). What was MISSING was a package to call, & itok publishes one now to the cache this flake ALREADY trusts ∴ the input costs a SUBSTITUTION, ⊥ a build. ⊥ A CYCLE though it reads like one: itok pins microlith at a TAG ∴ the graph TERMINATES. ABSENCE BEFORE FINDINGS (V36) & its EXIT CODE too: `command -v itok` FIRST, ∵ a missing tool & a breached ceiling BOTH exit non-zero & reporting the 1st as the 2nd sends a reader to COMPACT a file nothing MEASURED -- caught by the step doing exactly that on its 1st run. Then a 2nd catch, on MINE: I wrote the absence path `exit 1` & V36 says `says SO & exits 0` -- 0 + a LOUD line = EXAMINED NOTHING, which is ⊥ the same claim as a PASS & ! ⊥ be spelled like one. Fails CLOSED where it matters ∵ CI & the hooks run INSIDE the dev shell|V7,V17,V22,V36
T39|x|COMPACT SPEC.md back under a ceiling it EARNS, & the VERB comes first (V48, & V17 ∴ the runner is in this commit). T38 raised 23200 → 36800 ∵ the runner had to land somewhere TODAY, ⊥ ∵ the file earned the room. BUILT: `mth archive` + `archive_spec`/`archive_report` -- the 3rd mutation & the only one writing 2 files. The SHAPE was found by MEASUREMENT, ⊥ chosen: lifting rows out red-lines V15 through `claims_without_rows`, & the milestone-at-a-time fix qualified 3 of 12 milestones ∴ the STUB. RUN on our OWN spec: 28 of 33 done rows moved, 5 HELD on V16, 36757 → 29738 o200k ∴ the ceiling comes DOWN with it. PLANTED (V18): a held record · a 2nd run moving NOTHING · a fenced row · an id the archive ALREADY holds · & a COMPANION spec with no finished rows. The escape caught its own bug: `cells` leaves an escape INSIDE its field ∴ re-escaping doubled it, B36 one layer out, & the proof refused the write|V10,V12,V15,V16,V17,V18,V48
T40|x|the ROW CODEC lands as a SET & the three in-tree readings COLLAPSE to it (V43, & V17 ∴ the runner is in this commit). `unescape` becomes the SPLITTER's inverse -- it decoded only `\|` & left `\\` doubled; `escape` is ADDED ∵ a decoder with no encoder has one side under test & the other hand-written at every call site. `check::unescaped_pipes` → `cells().len()-1` & `migrate::table_cells` → `id::cells` ∴ ONE splitter, ⊥ three. `cells` · `escape` · `unescape` are EXPORTED at the root, which is ADDITIVE ∴ the release carrying it is a MINOR (V30). the effect is MEASURED rather than claimed & B36 carries both the method & the numbers|V7,V18,V42,V43
T41|x|the MILESTONE PARTITION is EXPORTED: `milestones(text)` → each `\| M<n> \|` row's id & its claimed task numbers, ranges expanded by `expand_cell`, in FILE order; `claims` becomes its flattening ∴ ONE reading of the tasks cell (V7). MEASURED NEED: a federated planner filtering one node's `§T` by milestone had only `claims` (the UNION, partition lost) & a crate-private module ∴ its only path was a 2nd reading of V15's grammar -- the pile §G names. ENUMERATE, ⊥ SELECT (V6): the partition is a FIELD of the spec, which milestone is next stays the caller's. REJECTED: a `milestone` field on `mth tasks --format json` alone -- a lib consumer would then parse JSON to learn what a pure fn can return; can follow later w/o breaking this|V6,V7,V15
